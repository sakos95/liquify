import { TestFunctionSources } from './test-function-sources';

describe('TestFunctionSources', () => {
  it('should create an instance', () => {
    expect(new TestFunctionSources()).toBeTruthy();
  });
});
